# services/trading-agents

Reserved directory — Python microservice (TradingAgents framework), served
via REST + WebSocket. See `docs/TRADE_BLUEPRINT_v2.md` Section 3 for the
architecture and Topic 6 (D6/R6, "AI Multi-Agent System") in
`docs/HANDOVER.md` for the build plan.

Nothing lives here yet. D6 builds the UI against fake responses; R6 wires
the real service. Do not add code here before D6/R6 unless a slice
explicitly calls for it.
